meta {
  name: Place Bet
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/bets
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "outcomeId": "{{outcomeId}}",
    "stake": 5
  }
}
